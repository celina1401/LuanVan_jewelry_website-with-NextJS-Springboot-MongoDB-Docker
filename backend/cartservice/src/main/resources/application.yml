server:
  port: 9005

spring:
  application:
    name: cartservice

eureka:
  client:
    service-url:
      defaultZone: http://discoveryserver:8761/eureka
    register-with-eureka: true
    fetch-registry: true 